#include <stdio.h>

/**
 * main - this is the main betty documentation function
 *
 * Return: 0 if need be
 */

int main(void)
{
	char i;

	for (i = 'z' ; i >= 'a' ; i--)
		putchar(i);
	putchar('\n');
	return (0);
}
