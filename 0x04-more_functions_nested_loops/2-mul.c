#include "main.h"

/**
 * mul - multiple two integers
 * @x: the first number to multiply
 * @y: the second number to multiply
 * Return: the result of the multiplications between x param and y param
 */

int mul(int x, int y)

{

	return (x * y);
}
